#include <iostream>
#include <string>
using namespace std;

#include "Cache.h"
using namespace Lab4;

int main(int argc, char *argv[])
{
    cout << "MAX_CAPACITY is "<< endl;


	cout << "MAX_CAPACITY is " << Cache::GetMaxCapacity() << endl;

	cout << "MAX_CAPACITY is " << Cache::GetMaxCapacity() << endl;
	cout << "MAX_CAPACITY is " << Cache::GetMaxCapacity() << endl;
}
